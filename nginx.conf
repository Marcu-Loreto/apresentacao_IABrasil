# nginx.conf
events {
    worker_connections 1024;
}

http {
    upstream api_backend {
        server api:8000;
    }

    upstream streamlit_backend {
        server streamlit:8502;
    }

    # HTTP (redireciona para HTTPS em produção)
    server {
        listen 80;
        server_name localhost qat.voxmap.neori.tech;

        # API
        location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Streamlit
        location / {
            proxy_pass http://streamlit_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 86400;
        }

        # WebSocket Streamlit
        location /_stcore/stream {
            proxy_pass http://streamlit_backend/_stcore/stream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400;
        }
    }
}
# ```

# ---

# ## 🎯 **ESTRUTURA COMPLETA DO REPOSITÓRIO**
# ```
# apresentacao_IABrasil/
# │
# ├── Dockerfile                 # FastAPI
# ├── Dockerfile.streamlit       # Streamlit
# ├── docker-compose.yml        # Orquestração (opcional)
# ├── nginx.conf                # Proxy reverso (opcional)
# │
# ├── api.py                    # Código FastAPI
# ├── app.py                    # Código Streamlit
# ├── shared_state.py          # Estado compartilhado
# │
# ├── requirements.txt         # Dependências Python
# ├── .env                     # Variáveis (NÃO versionar)
# ├── .env.example            # Exemplo de .env
# ├── .gitignore              # Ignorar arquivos
# │
# └── README.md               # Documentação